# Runway — Developer Guide

A year-at-a-glance planning tool — Gantt phases + daily task lists in an Electron desktop app.

---

## Install the packaged app

### Mac

1. Open `release/Runway-<version>.dmg`
2. Drag **Runway** to **Applications**
3. Launch from Applications or Spotlight

If macOS blocks the app ("unidentified developer"), right-click → Open → Open.

### Windows

1. Double-click `release/Runway Setup <version>.exe`
2. The app installs and launches automatically
3. Find it in the Start Menu as **Runway**

---

## Data storage

App data is stored in `localStorage` inside Electron. A plain-text backup is written automatically to:

- **Mac/Linux:** `~/Documents/runway/runway-backup.txt`
- **Windows:** `C:\Users\<you>\Documents\runway\runway-backup.txt`

Daily snapshots (kept for 30 days) are saved under `~/Documents/runway/daily backups/`.

Use **Export / Import** in the app to move data between machines.

---

## Developer setup

### Prerequisites

Install [Node.js](https://nodejs.org/) (v18 or newer). This includes `npm`.

Verify:

```
node --version
npm --version
```

### Install dependencies (first time only)

From inside the `Runway` folder:

```
npm install
```

---

## Run from source

Starts the Vite dev server and opens the Electron window. Live-reloads on code changes.

**Mac / Windows:**
```
npm run electron:dev
```

The app window opens automatically. Data is saved to `~/Documents/runway/`.

---

## Package (build a distributable)

### Mac → `.dmg` installer

Run on a Mac:

```
npm run electron:build:mac
```

Output: `release/Runway-<version>.dmg`

---

### Windows → `.exe` installer

**Option A — run on a Windows machine:**

```
npm run electron:build:win
```

**Option B — cross-compile from Mac (requires Wine + Mono):**

Install Wine via Homebrew:

```
brew install --cask wine-stable
```

Then:

```
npm run electron:build:win
```

Output: `release/Runway Setup <version>.exe`

---

### Both platforms at once

```
npm run electron:build:all
```

Produces both the `.dmg` and `.exe` in `release/`.

> **Note:** Building the Windows target from Mac requires Wine. Building the Mac target from Windows is not supported by electron-builder; use a Mac or a CI service (GitHub Actions, etc.).

---

## Make changes and repackage

1. Edit source files in `src/` or `electron/`
2. Test your changes in dev mode:
   ```
   npm run electron:dev
   ```
3. When ready, rebuild the package:
   ```
   npm run electron:build:mac   # Mac
   npm run electron:build:win   # Windows
   npm run electron:build:all   # Both
   ```
4. Distribute the new file from `release/`

---

## Project structure

```
Runway/
├── electron/
│   ├── main.cjs        # Electron main process (window, file I/O)
│   └── preload.cjs     # IPC bridge between Electron and the web app
├── src/                # React + TypeScript source
│   ├── App.tsx
│   ├── components/
│   ├── hooks/
│   └── utils/
├── dist/               # Built web app (generated by `npm run build`)
├── release/            # Packaged installers (generated by electron:build)
└── package.json
```
